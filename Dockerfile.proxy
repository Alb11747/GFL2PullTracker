FROM caddy:2.10.2-alpine
RUN addgroup -g 10001 tracker && adduser -D -u 10001 -G tracker tracker \
    && chown -R tracker:tracker /data /config
COPY docs/Caddyfile /etc/caddy/Caddyfile
USER 10001:10001
